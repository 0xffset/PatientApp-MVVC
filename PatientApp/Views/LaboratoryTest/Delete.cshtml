@using PatientApp.Core.Application.ViewModerls.LaboratoryTest
@model SaveLaboratoryTestViewModel
@{
    ViewData["Title"] = "Delete Laboratory Test";
}

<div class="row">
    <div class="col-6 offset-3">
        <div class="card">
            <div class="card-header bg-danger text-light">
                <h4>Are you sure want to delete @Model.Name</h4>
            </div>
            <div class="card-body">
                <form method="post" asp-controller="LaboratoryTest" asp-action="DeletePost">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger">Delete</button>
                    <a asp-controller="LaboratoryTest" asp-action="Index" class="btn btn-warning">Cancel</a>
                </form>

            </div>
        </div>
    </div>

</div>

